paths:
  /users/login:
    post:
      tags: 
      - Users
      summary: User Login
      requestBody:  
        content:
          application/json:
            schema: 
              $ref: '#/components/parameters/schemas/login'
        required: true
      responses:  
        200:
          description: Login success
          content: 
            application/json:
              schema: 
                $ref: '#/components/schema/Users/User'