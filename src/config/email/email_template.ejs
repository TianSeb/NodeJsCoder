<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Orden de compra completada</title>
  <style>
    /* Estilos para el cuerpo del correo */
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      margin: 0;
      padding: 0;
    }

    /* Estilos para el encabezado */
    header {
      background-color: #336699;
      padding: 20px;
      color: #fff;
      text-align: center;
    }

    /* Estilos para el contenedor principal */
    .container {
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
      background-color: #f9f9f9;
    }

    /* Estilos para la sección de "purchase order" */
    .purchase-order {
      background-color: #ffffff;
      border: 1px solid #ccc;
      border-radius: 5px;
      padding: 20px;
      margin-bottom: 20px;
    }

    /* Estilos para la lista de productos */
    .product-list {
      list-style-type: none;
      padding: 0;
    }

    /* Estilos para cada ítem de producto */
    .product-item {
      margin-bottom: 15px;
    }

    .product-name {
      font-weight: bold;
      color: #336699;
    }

    .product-details {
      margin-top: 5px;
    }

    /* Estilos para el total de la compra */
    .total-price {
      font-size: 24px;
      font-weight: bold;
      margin-top: 10px;
    }

    /* Estilos para el pie de página */
    .footer {
      background-color: #000;
      color: #fff;
      text-align: center;
      padding: 10px;
    }
  </style>
</head>
<body>
  <header>
    <h1>¡Gracias por tu compra!</h1>
  </header>

  <div class="container">
    <div class="purchase-order">
      <p>Hola <%= userEmail %>,</p>
      <p>Tu orden de compra ha sido completada con éxito.</p>
      <p>A continuación, se detallan los productos adquiridos:</p>
      <ul class="product-list">
        <% products.forEach((product) => { %>
          <li class="product-item">
            <span class="product-name"><%= product.name %></span> (Cantidad: <%= product.qty %>) - Precio unitario: $<%= product.price %>
            <div class="product-details">
              Total: $<%= product.total %>
            </div>
          </li>
        <% }); %>
      </ul>
      <div class="total-price">Monto total de la compra: $<%= totalPrice %></div>
      <p>¡Esperamos que disfrutes de tus productos!</p>
    </div>
  </div>

  <div class="footer">
    © 2023 Tienda en línea. Todos los derechos reservados.
  </div>
</body>
</html>
